export default function DealsPage() {
  return (
    <div className="p-8">
      <h1 className="text-3xl font-bold tracking-tight">Deals</h1>
      <div className="mt-6 bg-surface border border-line rounded-lg p-8 text-center text-sub">
        Coming soon — pipeline view not implemented yet.<br />
        Schema supports stages: lead → qualified → audit → proposal → closed.
      </div>
    </div>
  );
}
